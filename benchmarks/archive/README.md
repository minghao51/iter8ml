# Archived benchmark scripts

These scripts are **unmaintained legacy** from before the `tabular_blueprint` →
`iter8ml` rename. They still `import tabular_blueprint...` and will not run as-is;
they are kept only for historical reference.

The active benchmark suite is
[`benchmarks/run_openml_benchmark.py`](../run_openml_benchmark.py) (real OpenML /
scikit-learn datasets) with results rendered by
[`benchmarks/render_results.py`](../render_results.py).

Note: `notebooks/06_methodology_benchmarks.qmd` still references
`benchmarks.benchmark_models` / `benchmarks.benchmark_utils`; that notebook is
slated for refresh (see `docs/plan/portfolio-roadmap-20260805.md` §2.4).
